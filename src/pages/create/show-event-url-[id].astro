---
import Layout from '../../layouts/Layout.astro';
const { id } = Astro.params;
---

<Layout title={`スケジュール設定: ${id}`}>
 <main>
   <h1>スケジュール設定</h1>
   <p>以下のURLから出欠を入力できます。</p>
   <a class="schedule-link" href={`/answer/show-schedule-result-${id}`}>/answer/show-schedule-result-{id}</a>
 </main>
</Layout>

<style>
 main {
   margin: auto;
   padding: 2rem;
   max-width: 800px;
   background-color: #f5f5f5;
   color: #333;
   font-family: 'Noto Sans JP', sans-serif;
   text-align: center;
   border-radius: 8px;
   box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
 }
 h1 {
   font-size: 2.5rem;
   font-weight: bold;
   margin-bottom: 1.5rem;
   color: #4a90e2;
 }
 p {
   font-size: 1.2rem;
   margin-bottom: 2rem;
 }
 .schedule-link {
   display: inline-block;
   padding: 1rem 2rem;
   background-color: #4a90e2;
   color: #fff;
   text-decoration: none;
   border-radius: 4px;
   transition: background-color 0.3s ease;
 }
 .schedule-link:hover {
   background-color: #3377c8;
 }
</style>